
import ImageCards from '../ImageCard/ImageCard'

const ImageGallery = ({images}) => {
  return (
    <ul>
      {images.map((image) => (
        
      ))}
    </ul>

  )
}

export default ImageGallery